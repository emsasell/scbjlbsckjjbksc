'use client';import {useEffect,useState} from 'react';import type {ActionLog} from '@/lib/types';export default function AdminLogs(){const [rows,setRows]=useState<ActionLog[]>([]);useEffect(()=>{const load=async()=>{const r=await fetch('/api/admin/logs');if(r.ok)setRows(await r.json())};load();const t=setInterval(load,5000);return()=>clearInterval(t)},[]);return <main className="admin-wrap"><header className="admin-top"><a href="/admin">← Админ-панель</a></header><section className="admin-content standalone"><h1>Журнал действий</h1><p>Последние 10 действий.</p><div className="list">{rows.map(x=><article className="admin-item" key={x.id}><div><h3>{x.action}</h3><p>{x.details}</p><small>{new Date(x.created_at).toLocaleString('ru-RU')}</small></div></article>)}</div></section></main>}
