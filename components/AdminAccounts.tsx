'use client';import {useEffect,useState} from 'react';import type {User} from '@/lib/types';
export default function AdminAccounts(){const [users,setUsers]=useState<User[]>([]);const [f,setF]=useState<any>({login:'',password:'',display_name:'',role:'district',district_id:''});const [msg,setMsg]=useState('');async function load(){const r=await fetch('/api/admin/users');if(r.ok)setUsers(await r.json())}useEffect(()=>{load()},[]);async function save(e:any){e.preventDefault();const r=await fetch('/api/admin/users',{method:f.id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await r.json();if(!r.ok){setMsg(j.error||'Ошибка');return}setMsg('Сохранено');setF({login:'',password:'',display_name:'',role:'district',district_id:''});load()}async function edit(u:any){setF({...u,password:''})}async function del(id:number){if(confirm('Удалить аккаунт?')){await fetch('/api/admin/users',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()}}return <main className="admin-wrap"><header className="admin-top"><a href="/admin">← Админ-панель</a><a href="/">Сайт</a></header><section className="admin-content standalone"><h1>Аккаунты и доступы</h1><p>Пароли хранятся только в виде защищённых хешей: их нельзя безопасно показать после сохранения, но владелец может задать новый пароль.</p><form className="editor" onSubmit={save}><label>Логин<input required value={f.login} onChange={e=>setF({...f,login:e.target.value})}/></label><label>{f.id?'Новый пароль (оставьте пустым, чтобы не менять)':'Пароль'}<input required={!f.id} type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><label>Отображаемое имя<input value={f.display_name||''} onChange={e=>setF({...f,display_name:e.target.value})}/></label><label>Роль<select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="district">Создатель района</option><option value="admin">Администратор</option></select></label><label>ID привязанного района<input type="number" value={f.district_id||''} onChange={e=>setF({...f,district_id:e.target.value})}/></label><button className="primary">{f.id?'Сохранить':'Создать аккаунт'}</button>{msg&&<p>{msg}</p>}</form><div className="list">{users.map(u=><article className="admin-item" key={u.id}><div><h3>{u.login}</h3><p>{u.display_name||'Без имени'} · {u.role}{u.district_id?` · район #${u.district_id}`:''}</p></div><div className="row-actions"><button onClick={()=>edit(u)}>Изменить</button><button className="danger" onClick={()=>del(u.id)}>Удалить</button></div></article>)}</div></section></main>}
