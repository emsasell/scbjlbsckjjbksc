'use client';
import {useState} from 'react';
export default function Login(){const [p,setP]=useState('');const [e,setE]=useState('');const [loading,setLoading]=useState(false);async function go(ev:React.FormEvent){ev.preventDefault();setLoading(true);const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(r.ok) location.href='/admin';else {const j=await r.json();setE(j.error||'Ошибка');setLoading(false)}}return <main className="login"><div className="login-card"><img src="/avatar.jpg"/><span className="eyebrow">MEGAMINE CMS</span><h1>Вход в админ-панель</h1><p>Управляйте новостями, районами, вкладками и медиа.</p><form onSubmit={go}><input type="password" placeholder="Пароль администратора" value={p} onChange={e=>setP(e.target.value)} autoFocus/><button disabled={loading}>{loading?'Проверка…':'Войти'}</button></form>{e&&<div className="error">{e}</div>}<a href="/">← Вернуться на сайт</a></div></main>}
