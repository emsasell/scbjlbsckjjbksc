'use client';import {useState} from 'react';
export default function Login(){const [login,setLogin]=useState('0'),[p,setP]=useState(''),[e,setE]=useState(''),[loading,setLoading]=useState(false);async function go(ev:React.FormEvent){ev.preventDefault();setLoading(true);setE('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({login,password:p})});if(r.ok)location.href='/admin';else{const j=await r.json().catch(()=>({}));setE(j.error||'Ошибка');setLoading(false)}}return <main className="login"><div className="login-card"><img src="/avatar.jpg" alt=""/><span className="eyebrow">MEGAMINE CMS</span><h1>Вход в админ-панель</h1><form onSubmit={go}><input placeholder="Логин" value={login} onChange={e=>setLogin(e.target.value)} autoFocus/><input type="password" placeholder="Пароль" value={p} onChange={e=>setP(e.target.value)}/><button disabled={loading}>{loading?'Проверка…':'Войти'}</button></form>{e&&<div className="error">{e}</div>}<a href="/">← Вернуться на сайт</a></div></main>}
