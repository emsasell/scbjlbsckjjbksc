import {redirect} from 'next/navigation';import {isAdmin} from '@/lib/auth';import C from '@/components/AdminSettings';export default async function P(){if(!(await isAdmin()))redirect('/admin/login');return <C/>}
