'use client';import {useEffect,useState} from 'react';
export default function Create(){const [districts,setDistricts]=useState<any[]>([]),[f,setF]=useState<any>({title:'',body:'',district_id:'',published_at:''}),[msg,setMsg]=useState('');useEffect(()=>{fetch('/api/content').then(r=>r.json()).then((a:any[])=>setDistricts(a.filter(x=>x.kind==='district'))).catch(()=>{})},[]);async function submit(e:any){e.preventDefault();const r=await fetch('/api/submissions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await r.json();setMsg(r.ok?'Новость отправлена. Если вы создатель района, она появится после одобрения администратора.':j.error||'Ошибка')}return <main className="auth-page"><section className="auth-card"><h1>Создать публикацию</h1><p>Правила: публикуйте только достоверную информацию о районе; запрещены оскорбления, спам, чужие персональные данные и незаконный контент. Администратор может одобрить или отклонить публикацию.</p><form onSubmit={submit}><input required placeholder="Название новости" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><textarea required placeholder="Текст новости" value={f.body} onChange={e=>setF({...f,body:e.target.value})}/><select required value={f.district_id} onChange={e=>setF({...f,district_id:e.target.value})}><option value="">Выберите район</option>{districts.map(d=><option key={d.id} value={d.id}>{d.title}</option>)}</select><input type="datetime-local" value={f.published_at} onChange={e=>setF({...f,published_at:e.target.value})}/><button>Отправить на публикацию</button></form>{msg&&<p>{msg}</p>}<a href="/">← На сайт</a></section></main>}
