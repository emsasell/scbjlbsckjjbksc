import {NextResponse} from 'next/server'; import {adminCookie,authenticate} from '../../../../lib/auth';
export async function POST(req:Request){const {username,password}=await req.json().catch(()=>({}));const user=await authenticate(String(username||''),String(password||''));if(!user)return NextResponse.json({error:'Неверный логин или пароль'},{status:401});const res=NextResponse.json({ok:true,username:user.username});res.cookies.set(adminCookie(user));return res}
