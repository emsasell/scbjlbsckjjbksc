'use client';import {useState} from 'react';export default function Login(){const [login,setLogin]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/account/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({login,password})});const j=await r.json();if(!r.ok)return setMsg(j.error||'Ошибка');location.href=j.role==='admin'||j.role==='owner'?'/admin':'/create'}return <main className="auth-page"><form className="auth-card" onSubmit={go}><h1>Вход</h1><input placeholder="Логин" value={login} onChange={e=>setLogin(e.target.value)}/><input type="password" placeholder="Пароль" value={password} onChange={e=>setPassword(e.target.value)}/><button>Войти</button>{msg&&<p>{msg}</p>}</form></main>}
